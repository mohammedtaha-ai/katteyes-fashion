FROM node:20-alpine
WORKDIR /app

EXPOSE 5173

# دائماً npm install (idempotent — سريع لو مفيش تغيير في package.json)
CMD ["sh", "-c", "npm install --no-audit --no-fund && npm run dev -- --host 0.0.0.0"]
